export enum AccessLevelType {
  EVERYONE = 1,
  GOLD = 2,
  VIP = 3,
}

export enum TypeOfVideo {
  EMBED = 1,
  UPLOAD = 0,
}

export enum TypeOfImagePost {
  SINGLE = 1,
  MULTIPLE = 2,
}

export enum PostType {
  "image" = "image",
  "video" = "video",
  "audio" = "audio",
  "text" = "text",
}
